# Diese Datei wird von setup.sh automatisch aus nginx-host.conf.template
# mit der konfigurierten Domain generiert.
#
# Manuell generieren:
#   sed "s/YOUR_DOMAIN/deine.domain.de/g" deploy/nginx-host.conf.template \
#     > /etc/nginx/sites-available/tesla-carview
#
# Vollständige Konfiguration: deploy/nginx-host.conf.template
