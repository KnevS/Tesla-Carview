# Copy to update-webhook.env (gitignored) and fill in.
# Generate: openssl rand -hex 24

UPDATE_WEBHOOK_SECRET=your-random-secret-here

# Optional:
# UPDATE_WEBHOOK_PORT=7071
# UPDATE_WEBHOOK_BIND_HOST=172.18.0.1
# UPDATE_SCRIPT_PATH=/opt/tesla-carview/deploy/update.sh
