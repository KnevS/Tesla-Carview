# CLAUDE.md — Repo-Anweisungen für KI-Assistenten

Diese Datei wird von Claude Code (und vergleichbaren KI-Agenten) bei
jeder Session in das Kontext-Fenster geladen. Sie ergänzt die normalen
Coding-Konventionen um **harte Regeln zum Schutz privater Daten**, weil
dieses Repo öffentlich ist.

## Tesla Carview ist ein Public-Repo

`github.com/KnevS/Tesla-Carview` — PolyForm Noncommercial 1.0.0.
Alles in `main` ist weltweit lesbar und wird vermutlich gespiegelt /
gecrawlt / im Web-Archive landen. **Was einmal drin ist, ist drin.**

## Was NIE committet werden darf

### Geheime Zugangsdaten
- Tesla OAuth Access-Tokens, Refresh-Tokens, Client-Secrets
- JWT_SECRET, SESSION_SECRET, Cookie-Keys (Backend `.env`)
- Google-Maps API-Keys, Tibber/aWattar Tokens
- VAPID-Private-Key (WebPush)
- TLS / SSH / GPG Private-Keys (`*.key`, `*.pem`, `*.p12`)

### Identifizierende Personendaten
- Echte VINs (17-stellig, beginnt mit `5YJ` / `7SA`)
  → Doku/Tests immer mit Demo-VIN `5YJ3E1EA1NF000000` arbeiten
- Operator-Mailadresse außerhalb von
  README / NOTICE / AUTHORS (dort als Attribution gewollt)
- Konkrete Lat/Lon-Koordinaten mit > 4 Nachkommastellen aus
  Live-Fahrten (Demo-Seeder rundet bewusst auf 4)
- Reale Klar-IP-Adressen aus Logs / Tests
- Reale Benutzernamen, Telefonnummern, Adressen von Testern

### Datenbank-Inhalte
- `*.db`, `*.sqlite` — Tenant-DBs enthalten Live-Daten
- `data/`-Verzeichnis komplett (enthält auch `.encryption-key`, s. u.)
- DB-Dumps, SQL-Snapshots mit echten Rows
- Backup-JSONs aus dem Restore-Endpoint

### Encryption-Key
- `data/.encryption-key` (32 Bytes, 0600) — entschlüsselt Tesla-Tokens,
  MFA-Secrets und Virtual-Key Private-Keys at-rest. **Nie ins Repo,
  nie in Logs, nie in Issues.** Backup zusammen mit `data/`.
- `data/.ledger-key` (32 Bytes, 0600) — HMAC-Signaturschlüssel des
  manipulationssicheren Fahrtenbuchs (`trip_ledger`, S09). **Nie ins Repo,
  nie in Logs.** Backup zusammen mit `data/`; ohne ihn ist die Änderungs-
  Chain nicht mehr verifizierbar.

### Build- und Runtime-Artefakte
- `.env`, `.env.local`, `.env.production` (nur `.env.example` ist OK)
- `node_modules/`, `dist/`, `.vite/`, `coverage/`
- Crash-Dumps, `*.log`

## Wenn ich (Claude) unsicher bin

**Lieber fragen als raten.** Bei Beispiel-Daten in Tests, Docs oder
Migrations: vor dem Commit kurz nachfragen, ob die konkrete Zahl /
ID / Adresse ein Platzhalter oder echte Daten ist.

Standard-Platzhalter, die immer OK sind:
- VIN: `5YJ3E1EA1NF000000`
- E-Mail: `user@example.com`, `tester@example.org`
- Tenant-Slug: `demo`, `acme`, `default`
- Tesla-ID: `123456789`
- Lat/Lon: `52.5200, 13.4050` (Berlin Mitte, vier Nachkommastellen)

## Sicherheitsnetze, die ohnehin greifen

1. **`.gitignore`** blockiert `.env`, `data/`, `*.db`, `*.key`, `*.pem`,
   `*.log` schon auf der Indexer-Ebene.
2. **Pre-Commit Hook** (`scripts/git-hooks/pre-commit`) ruft `gitleaks`
   gegen `.gitleaks.toml` auf — fängt VINs, Tokens, Operator-E-Mail
   noch lokal ab.
3. **GitHub Push Protection** blockiert Pushes mit bekannten
   Token-Patterns serverseitig.
4. **CI-Workflow** (`.github/workflows/gitleaks.yml`) scannt nochmal
   im Build — letzte Reissleine.

**Trotzdem gilt:** Diese Netze fangen Bekanntes ab. Wenn ich ein
neues Geheimnis-Pattern erfinde, fängt es niemand außer aufmerksamem
Lesen. Deshalb diese Datei.

## Commit-Konventionen

- Sprache: Commit-Messages auf **Englisch** (Repo geht an
  internationale Audience)
- Code-Kommentare bleiben **deutsch** (siehe Memory)
- Commit-Author: `Sven Krische <15202551+KnevS@users.noreply.github.com>`
- Co-Author-Line am Ende: `Co-Authored-By: Sven Krische <…>`
- Format: `<type>(<scope>): <subject>`, type ∈ `fix|feat|chore|docs|refactor`

## Workflow: PR-Pflicht (seit 2026-07-09)

Änderungen laufen über Feature-Branches und Pull Requests — **kein
Direkt-Push auf `main` mehr** (einzige Ausnahme: das GitHub-Wiki, das
technisch keine PRs unterstützt). Gilt auch für `teslaview-web`.

1. Branch `feat/…` / `fix/…` / `docs/…` / `chore/…` von aktuellem `origin/main`
2. Commits nach den Konventionen oben; Version-Bump + CHANGELOG DE/EN
   gehören in denselben PR
3. PR öffnen (`gh pr create`) — CI, Security und Secret-Scan müssen grün sein
4. Squash-Merge (`gh pr merge --squash --delete-branch`) — Historie bleibt
   linear, PR-Nummer landet im Commit-Titel
5. Deploy läuft wie gehabt automatisch nach dem Merge auf `main`

**Vor jedem Commit/PR** (unverändert):

1. `git diff --cached` reviewen, nicht nur die Summary
2. Auf Dateinamen achten, die nach Test-Fixtures aussehen
3. Bei Markdown-Änderungen: hat sich ein realer Datensatz
   eingeschlichen?
4. Backend-Logs in Code-Blocks → IPs / VINs / Tokens redacten


---

## Aktueller Entwicklungsstand

> ⚠️ **Dieser Abschnitt darf nie veralten.** Jede Session, die am Repo arbeitet,
> aktualisiert die „Aktuell"-Zeilen unten (Version + zuletzt geliefert), bevor sie
> fertig meldet. Im Zweifel gelten die verbindlichen Live-Quellen, NICHT dieser Text:
> - **Version:** `backend/package.json` + `frontend/package.json` (Single Source of Truth, auch live unter `/api/system/version`)
> - **Was zuletzt geschah:** oberster Eintrag in [`CHANGELOG.md`](CHANGELOG.md) / [`CHANGELOG.en.md`](CHANGELOG.en.md)
> - **Letzte Commits:** `git log --oneline -15 origin/main`

### Aktuell (Stand 2026-09-01)

- **Version:** v3.56.0
- **Zuletzt geliefert:**
  - **v3.56.0 (kWh im Zeitraum + Ø-Verbrauch war unbrauchbar):** Sven-Rückfrage „wo sehe ich die kWh". Die Zeitraum-Leiste zeigt jetzt auch `total_energy_kwh`. Dabei fiel die Kachel daneben auf: `/api/trips/stats` rechnete `AVG(kWh ÷ km)` **über die Einzelfahrten**, also ungewichtet — eine 20-Meter-Aufzeichnung mit 0,75 kWh (rechnerisch 3426 kWh/100 km) zählte wie eine Autobahnetappe. Auf der Referenzinstanz meldete die Kachel dadurch 39,9 statt 21,0 kWh/100 km, im gewählten Zeitraum sogar 96. Jetzt `SUM(energy) ÷ SUM(distance) × 100`; Fahrten ohne Energiewert bleiben aus **beiden** Summen draußen. Zusätzlich neu: `energy_trips` im Stats-Endpunkt — trägt nicht jede Fahrt einen Energiewert (Referenzinstanz: 230 von 494), bekommt die kWh-Zahl ein `*` und der Tooltip nennt das Verhältnis, sonst liest sich eine Teilsumme wie der Gesamtverbrauch. **Merke:** Ein arithmetisches Mittel über Quotienten ist bei stark streuenden Nennern (Fahrtstrecken!) keine Kennzahl, sondern ein Zufallswert. `routing.js`/`insightEngine.js`/`community.js` benutzen dieselbe Form, filtern aber `distance_km > 1..2` und blieben deshalb bewusst unverändert.
  - **v3.55.0 (Heatmap-Fahrwege unsichtbar + Nav-Auswahl hielt nicht):** Zwei Meldungen aus dem Betrieb. Fahrwege lagen auf blassem Violett `#a78bfa`, 2 px, 55 % Deckkraft — auf hellen OSM-Kacheln unsichtbar, gerade bei Überlagerung. Jetzt Magenta `#c026d3`, 3 px, 80 %, mit `localStorage`-Migration des alten Defaults (sonst hätte kein Bestandsnutzer die Änderung gesehen). Zweitens: „Einstellungen → Navigationsleiste anpassen" schrieb Reihenfolge/Sichtbarkeit **nur** in den `localStorage`, während `SettingsWizard.vue` dieselben Werte als `nav_order`/`nav_hidden` ins Profil legt — `prefs.load()` → `_applyToStores()` spielte den Profilstand bei jedem Start darüber. `Settings.vue` schreibt jetzt beide Seiten. **Merke:** Zwei Schreibpfade für denselben Zustand sind immer ein Bug, auch wenn beide für sich „funktionieren".
  - **v3.54.0 (Zeitraum-Filter + Markierungs-Summe in der Fahrtenliste):** Sven-Wunsch „Zeitraum angeben oder Markierungen setzen, um die Gesamtkilometer der Auswahl auszugeben". Zeitraum-Leiste (Schnellwahl + freie Von/Bis-Grenzen, in lokaler Zeit gerechnet → kein UTC-Versatz) mit serverseitiger Summe; Markier-Modus mit fixer Summenleiste. **Vier Altlasten mitgefixt, alle aus derselben Wurzel — Liste und Statistik waren sich über den Filter nicht einig:** `GET /api/trips` las `trip_type` **nie** (das Typ-Dropdown tat schlicht nichts), `/trips/stats` bekam nur `vehicle_id` (die Kacheln ignorierten Fahrer- und Typ-Filter), der Datums-Link der Aktivitäts-Heatmap (`/trips?date=…`, im Handbuch beschrieben) lief ins Leere, und `/trips/logbook` hatte **keinen** `restrictToOwnVehicles`-Guard — ein normaler Nutzer konnte darüber Fahrten fremder Fahrzeuge desselben Mandanten lesen. Alle Fahrten-Endpunkte teilen sich jetzt `tripFilter()`. **Merke:** Eine paginierte Liste taugt nie als Summenquelle; die Summe gehört serverseitig neben denselben Filter.
  - **v3.53.0 (Windows als Host — Docker Desktop + WSL2):** Anlass war eine Interessenten-Anfrage („möchte TeslaView auf Windows betreiben"). Die App steckt ohnehin komplett in Linux-Containern; es fehlten der Weg dorthin und die Kenntnis der Fallstricke. **Drei Dinge empirisch geklärt (auf iland, Wegwerf-Compose-Projekt, danach aufgeräumt), nicht vermutet:** (1) **`host.docker.internal` ist der eigentliche Blocker** — Docker Desktop schreibt den Namen selbst in `/etc/hosts` jedes Containers, und der Eintrag **gewinnt gegen den Netzwerk-Alias**: gemessen 172.17.0.1 (Host-Gateway) statt Proxy-Container. Signed Commands wären still ans falsche Ziel gegangen. `PROXY_BASE` in `teslaApi.js` war hart verdrahtet → jetzt `TESLA_PROXY_BASE`-überschreibbar, Linux-Default unverändert. (2) **Fehlende Bind-Mount-Quelle blockiert NICHT** — Docker legt ein leeres Verzeichnis an, der Container startet und scheitert erst am fehlenden Key. (3) **`depends_on: {required: false}` + `profiles: [disabled]` funktioniert** — mit Windows-Override enthält der Stack nur backend/frontend/nginx, ohne „depends on undefined service"-Abbruch. Zusätzlich geprüft: Compose verträgt CRLF **und** UTF-8-BOM in `env_file` (die naheliegende Sorge trifft also nicht zu), und **Compose interpoliert `${...}` NUR aus der Projektwurzel-`.env`, nie aus `backend/.env`** — deshalb neu ein `.env.example` in der Wurzel. Geliefert: `deploy/setup-windows.ps1` (JWT-Secret via `RandomNumberGenerator::Create()` — nicht `::Fill`, das gibt es in Windows PowerShell 5.1 nicht), `TESLA_PROXY_CONFIG_DIR`/`_UID`/`_GID` statt hart kodierter Werte, docs/02-deployment ×7 (Plattform-Tabelle + Fußnote + eigener Abschnitt), README ×7. **Bewusst nicht gebaut:** Windows-eigene HTTPS-Lösung (Tesla-Anforderung gilt für jede Heiminstallation, steht in `14-network-access.*`). **Ehrlich ausgewiesen: nicht CI-getestet** — das Build-Gate baut Linux-Images, ein Windows-Host wird nirgends automatisiert geprüft.
  - **v3.52.5 (Doku-Hygiene zu v3.52.4 — Proxy-Modus verschwieg die Rate-Limits):** Sven-Rückfrage „hygiene auch erledigt?". Befund: `setup.sh` hat einen **Proxy-Modus** (`DEPLOY_MODE=2`), der bewusst keine nginx-Config anlegt und bisher nur `proxy_pass http://127.0.0.1:8080;` ausgab — welche Limits der eigene Proxy nachbauen muss, stand nirgends. `docs/02-deployment.*` erwähnte den Proxy-Modus überhaupt nicht (0 Treffer auf „proxy" in allen 7 Sprachen). **Damit war die Anleitung selbst der Weg in den v3.52.4-Bug.** Geschlossen an drei Stellen: `setup.sh` gibt die drei Limits direkt beim Setup aus; `docs/02-deployment.*` ×7 neuer Abschnitt „Betrieb hinter einem eigenen Reverse-Proxy" (Tabelle + Vorlagen-Verweis + 429-Erkennungsregel); Handbuch ×6 Fehlerbehebungs-Eintrag. Wiki: `*-Installation.md` (Proxy-Hinweis erweitert) + `*-Troubleshooting.md` direkt gepflegt. **Bewusst NICHT angefasst:** README ×7 — dort geht es um Funktionen, nicht Betrieb (die „Proxy"-Treffer dort betreffen `tesla-http-proxy`, geprüft statt angenommen). **Merke:** Bei einem Betriebs-Bug ist die Doku-Hygiene nicht README/Feature-Liste, sondern der Pfad, den ein Betreiber tatsächlich abläuft — hier Setup-Ausgabe → Deployment-Doku → Wiki-Installation.
  - **v3.52.4 (Seitenwechsel brauchte oft einen Reload — 429 vom Reverse-Proxy):** Sven-Report „in teslaview muss ich oft die Seite neu laden um eine andere Seite innerhalb zu laden". **Gemessen statt vermutet:** 40 parallele Requests auf `/api/health` → 20× 200 / 20× 429; Header `x-retry-in` (Traefik-Signatur) wies die Drosselung dem vorgelagerten Traefik zu, während die App zeitgleich `ratelimit-remaining: 93` von 120 meldete. Der Router `teslaview-api` (`PathPrefix(/api/)`) hing an `rl-api` = average 120/min, **burst 20** — und dieser Pfad umfasst `/api/tiles/`. Damit wiederholte sich auf der Traefik-Ebene exakt der Fehler, der für nginx seit PR #213 (`zone=tiles`) behoben ist; die Host-nginx-Config trägt den Fix, war aber gar nicht mehr im Anfrageweg (Traefik terminiert TLS, `docker-compose.override.yml` nimmt den Host-Port 8080 raus). Ein Karten-Zoom (50–150 Kacheln) leert den Eimer sofort — aber schon ein normaler Seitenwechsel reicht: Dashboard ~15 Calls inkl. Dauer-Widgets, Laden/Batterie je +11, Routenplaner +18. **Code-Fix:** `api.js` behandelte 429 überhaupt nicht (Promise rejectete, View blieb leer, Reload wirkte wie die Lösung). Jetzt max. 3 Retries mit Wartezeit aus `x-retry-in`/`retry-after`, sonst exponentiell, gedeckelt auf 4 s, **alle über einen gemeinsamen Takt um 150 ms gestaffelt** (sonst laufen 20 abgewiesene Requests gleichzeitig erneut ins Limit). Neu: `ThrottleNotice.vue` (i18n ×7, bewusst ohne Ursachen-Behauptung — der Client kann Proxy und App nicht unterscheiden), `deploy/traefik-dynamic.example.yml` + Rate-Limit-Tabelle in `deploy/README.md`. **Host-Seite (nicht im Repo, manuell):** `rl-tiles`-Middleware + Router `teslaview-tiles` (priority 115) und `rl-api` burst 20 → 60 in `/opt/traefik/dynamic/`. **Merke:** Nicht die Dauerrate war falsch, sondern der Burst — und ein an einer Stelle korrigiertes Limit muss bei jedem Wechsel der Edge-Schicht mitwandern.
  - **v3.52.3 (Grüne Wochenreports blieben als offene Issues liegen):** Der wöchentliche Commit-Quality-Report legt auch bei fehlerfreiem Ergebnis ein Issue an („✅ … Alle Commits konform") — geschlossen hat sie niemand, drei lagen seit Wochen offen (#256, #264, #278). Eine Issue-Liste, in der Erfolge stehen bleiben, verliert genau die Aussagekraft, für die sie da ist. Altfälle geschlossen (Nachweis bleibt darin lesbar) + neuer Workflow `report-autoclose.yml`: Report mit Label `quality`/`weekly-report` und führendem ✅ im Titel wird beim Anlegen sofort mit Begründung geschlossen, alles andere bleibt unangetastet offen. **Bewusst titel- und labelbasiert statt autorbasiert** — der Report kommt von einem Agenten unter einem echten Nutzerkonto, ein Autorfilter würde jedes handgeschriebene Issue miterfassen. Der Issue-Titel geht ausschließlich über `env:` in den Step (nie in eine Kommandozeile interpoliert); Erkennung gegen echte und bösartige Titel verifiziert. **Merke (gilt repo-weit):** Ein Automatismus, der bei Erfolg eine Meldung hinterlässt und sie nicht selbst wieder aufräumt, erzeugt Rauschen statt Information — `docker-build-gate.yml` macht es richtig und schließt sein Issue, sobald der Lauf wieder grün ist.
  - **v3.52.2 (`docker-compose.prod.yml` überschrieb das Backend-Image):** `backend/src/routes/system.js` war als Single-File-Bind-Mount eingehängt und gewann damit immer gegen die Datei im Image — die Referenzinstallation lief mit 43 statt 46 Routen (u. a. fehlten `self-check` und `telemetry-backfill`), ohne dass ein Deploy je einen Fehler meldete. Mount ersatzlos entfernt.
  - **v3.52.1 (Rotation/Selbsttest vs. verschlüsselte Sicherungen):** `sanitize-backups.sh` hängt serverseitig `.enc` an; Rotation filterte strikt auf `.json`/`.db.gz` und hätte nach dem ersten Verschlüsselungslauf **nie wieder** eine Datei gefunden (unbegrenztes Wachstum, ~115 MB/Nacht bei 3 GB frei). Beide Varianten werden jetzt akzeptiert; bei verschlüsselter Sicherung meldet die Integritätsprüfung ehrlich, dass sie ohne Schlüssel nur Vorhandensein und Größe beurteilen kann.
  - **v3.52.0 (Nächtlicher DB-Snapshot als tragfähiger Restore-Weg):** Das JSON-Backup ließ sich erzeugen, aber nicht mehr zuverlässig einspielen (405 MB Spitze bei 92 MB Datei, 590 MB Container-Limit). `VACUUM INTO` + streamendes Gzip liefert einen transaktional konsistenten Snapshot bei 137 MB Spitze. Neuer Selbsttest-Check `backup_snapshot` prüft bewusst die **Datei auf der Platte** statt eines Statusfelds — genau so ein Feld meldete im Juli 17 Tage lang fälschlich Erfolg.
  - **v3.51.12 (Auto-Backup riss jede Nacht das Backend um — 17 Tage kein Backup):** `buildBackupPayload()` lud alle 27 Tabellen als Objektgraph in den Heap, `JSON.stringify()` legte eine zweite Kopie daneben → ~31 OOM-Abstürze pro Nacht. Jetzt zeilenweise per `.iterate()` auf Platte (92 MB Datei bei 31 MB Heap). Zusätzlich: Tagesmarke `backup.last_attempt_day` persistent und **vor** dem Lauf gesetzt (verhinderte die stündliche Absturzschleife), `backup_recent`-Selbsttest reparierte einen `Number(ISO-String)` → `NaN`-Vergleich, der dauerhaft ohne Aussagewert auf „warn" stand.
  - **v3.51.11 (Fix: Telemetry-Fahrten ohne SoC/Odometer/Verbrauch + Backfill):** Sven-Report „Strecke 20,0 km, Verbrauch –, SoC –% → –%". Diagnose auf dem Live-Server: Die Fahrt (Trip 276, `source='telemetry'`) hatte 578 `telemetry_points`, davon 125 mit SoC (68 → 66) und 49 mit Odometer — die Werte waren also **vorhanden**, nur nicht in der Fahrt. Zwei gestapelte Ursachen: 1) `storePoint()` las `start_/end_soc`+Odometer nur aus genau der Nachricht, die den Gear-Wechsel meldet — Tesla streamt SoC/Odometer aber in separaten Nachrichten (für Lat/Lon existierte deshalb schon ein COALESCE-Nachzug aus den Trackpunkten, für SoC/Odometer nicht); 2) `energy_used_kwh` berechnete ausschließlich der Polling-Pfad (`dataSync.finishOdometerTrip`), und der Poller stand seit dem Vormittag wegen Monats-Cap still (Log: „Otto II: Monats-Cap (1000 Calls) erreicht … Polling bis naechsten Monat gestoppt" — Cap resettet am 01.08., ist aber seit diesem Fix für SoC/Verbrauch egal, solange der Telemetry-Stream läuft). Fix: `enrichClosedTrip()` beim Fahrt-Ende (SoC/Odometer aus erstem/letztem non-NULL-Trackpunkt, Verbrauch aus SoC-Delta × 75 kWh wie dataSync, nie Überschreiben vorhandener Werte) + `backfillTelemetryTrips()` für Bestandsfahrten (nächtlich in `nightlyMaintenance` Task 3d + `POST /api/system/telemetry-backfill`, Admin-only/auditiert). **Bewusst kein `trip_ledger`-Eintrag:** System-Schreibpfade (dataSync, Telemetry-Close, OwnTracks) ledgern grundsätzlich nicht — der Ledger sichert User-Edits ab; das Muster wurde beibehalten. **Merke:** `distance_km` bleibt GPS-Haversine (19,99 km) und wird NICHT auf das Odometer-Delta (20,24 km) umgestellt — bewusst unangetastet, um die Semantik bestehender Fahrten nicht zu ändern. QA gegen einen `better-sqlite3 .backup()`-Snapshot der echten Tenant-DB (nie gegen die Live-DB).
  - **v3.51.10 (`wiki-sync.yml`-Workflow entfernt — Datenverlust-Bug):** Beim Pushen von v3.51.9 fiel auf, dass ein Re-Run eines alten Workflow-Laufs (Bezug PR #245, 23.07.) das GitHub-Wiki gerade blind mit der seit Monaten veralteten `.github/wiki/*.md`-Quelle überschrieben und dabei in allen 7 Sprachen ganze Feature-Sektionen gelöscht hatte (Dienstwagen-Steuer, Manipulationssicheres Fahrtenbuch, Fahrtwerte/Heatmap/Zonen-Analyse, App-Hub, Ladeplaner, Erstattung, PV-Überschuss, SoH-Zertifikat) — der Workflow überschreibt (`cp`) statt zu mergen, und die eingecheckte Quelle spiegelte nie die laufende direkte Wiki-Pflege wider. Inhalt noch am selben Tag im Wiki-Repo repariert (Merge-Konflikt-Auflösung zugunsten des bestehenden Wiki-Stands + Übernahme der legitimen Pi-4-Doku-Änderungen). **Sven-Entscheidung nach Rückfrage:** Workflow + `.github/wiki/*.md` ersatzlos entfernt statt repariert — die tatsächliche Praxis (Direkt-Edits im separaten Wiki-Repo) ist ohnehin die einzig gültige Quelle, ein zweiter veralteter Snapshot ist nur ein wiederkehrendes Risiko. **Merke:** Das GitHub-Wiki wird ab sofort ausschließlich direkt im `Tesla-Carview.wiki`-Repo gepflegt, es gibt keine automatische Sync-Quelle mehr im Hauptrepo.
  - **v3.51.9 (Doku-Sync nachgezogen für S10 + Dienstwagen-BEV-Umstellung):** Sven-Rückfrage „hygiene auch gemacht?" deckte auf, dass die 5 Routenplaner-Features (v3.51.4–.8) und die BEV-only-Reduktion der Dienstwagen-Versteuerung (v3.51.3) zwar im CHANGELOG/CLAUDE.md, aber nicht vollständig in Handbuch/README/Wiki dokumentiert waren — Verstoß gegen die eigene Regel „Doku im selben Arbeitsgang wie das Feature". Nachgezogen: Handbuch ×6 (Routenplaner-Sektion um Gesamtreisezeit/Teilstrecken, Pausen, Anbieter-Filter, Community-Kommentare erweitert; Dienstwagen-Sektion DE/EN auf BEV-only reduziert), README ×7 (Feature-Zeilen ergänzt), Wiki `Features.md` ×7. **Bewusst NICHT nachgeholt:** Dienstwagen-Versteuerung für ES/FR/TR/EL — die Sektion existierte dort von Anfang an nicht (deutsches Steuerthema, wie bei v3.45.0 bewusst auf DE/EN begrenzt) — kein durch diese Session verursachter Rückstand, daher keine Neuübersetzung im Rahmen der Hygiene. Handbuch/Wiki-Sprachparität (EN/DE/UK vollständig, ES/FR/TR/EL fehlen 8–11 unabhängige Sektionen wie Kostensplit/Energiebilanz/Preisranking/Ladewirkungsgrad) bleibt als separates, größeres Übersetzungs-Backlog offen — nicht Teil dieser Session.
  - **v3.51.8 (Routenplaner: Community-Kommentare zu Ladestationen — fünftes und letztes Ergebnis des Routenplaner-Wunschzettels, S10 abgeschlossen):** Ladestations-Popup lädt beim Öffnen (`popupopen`-Event, gecacht pro Station) bis zu 3 aktuelle OpenChargeMap-Kommentare nach (Autor/Datum/Text) — neuer Endpoint `GET /api/routing/chargers/:id/comments` (`chargepointid`+`includecomments=true`, gezielt statt bulk bei `/chargers` aus Performance-Gründen). **Sven-Entscheidung nach Rückfrage:** keine eigene Bewertungs-DB-Migration — TeslaView ist Multi-Tenant-Selfhoster ohne zentralen Server, echte Cross-Instanz-Community bräuchte einen den es nicht gibt; OCM selbst hat zudem kein strukturiertes Amenity/WC-Feld, nur Freitext-`UserComments` (verifiziert gegen die offizielle OCM-OpenAPI-Spec). **Sicherheitskritisch:** Community-Freitext ist unkuratierter Fremdinhalt, der roh ins Leaflet-Popup-HTML interpoliert wird — `escapeHtml()` auf alle Werte (Name, Anbieter, Kommentarautor, -text) angewendet, live mit `<script>alert(1)</script>`-Testkommentar verifiziert (landet als Text `&lt;script&gt;`, kein ausführbares DOM-Element). i18n ×7 (4 neue Keys). **Merke Playwright+Leaflet:** `.click()` auf `.leaflet-marker-icon` ist unzuverlässig und öffnet manchmal ein Popup mit falschem Inhalt statt gar keins — kostete hier lange Fehlersuche im (tatsächlich korrekten) eigenen Code, bis `dispatchEvent(new MouseEvent('click', {...}))` direkt aufs Icon-Element den Bug im Testwerkzeug statt im Code entlarvte; Details in Memory `project-tcv-local-repro`. **S10-Roadmap Routenplaner: alle 5 Punkte geliefert und gemergt** (#251–#255) — 1) Gesamtfahrzeit + Teilstrecken, 2) Ladedauer-Forecast/kWh, 3) Pausen-Eingabe, 4) Anbieter-Filter, 5) Community-Kommentare (dieser Eintrag).
  - **v3.51.7 (Routenplaner: Anbieter-Filter für Ladestationen — viertes Ergebnis des Routenplaner-Wunschzettels, S10):** Neue Chip-Leiste „Nach Anbieter filtern" (nur sichtbar bei ≥ 2 gefundenen Anbietern). Wirkt zweigleisig: clientseitig sofort auf die Kartenanzeige (`filteredChargers` computed, `updateChargerMarkers()` umgestellt) UND serverseitig auf die Ladeplan-Berechnung (`allowed_operators` in `POST /api/routing/plan`, filtert `allChargers` vor `planChargingStops()`, `chargers_found` zeigt jetzt die gefilterte statt die rohe Anzahl). **Interaktionsmodell:** leere Auswahl = alle Anbieter erlaubt (Standard), ein Klick auf einen Chip schränkt auf GENAU diesen einen Anbieter ein (Inklusions-Filter, kein Ausschluss-Toggle) — Backend hatte `operator` pro Charger schon länger (`s.OperatorInfo?.Title`), nur nie zum Filtern genutzt. **Diagnose-Weg:** lokal kein OpenChargeMap-API-Key konfiguriert (403 „NO_API_KEY" bei echtem Aufruf) → `/api/routing/chargers` per Playwright-Route-Mock mit 4 synthetischen Stationen (2× EnBW, 1× Tesla Supercharger, 1× Ionity) simuliert, „EnBW" ausgewählt → Kartenmarker sanken von 5 auf 3, bestätigt korrekt. **Merke:** Such-Selektoren auf Button-Text sind fragil, wenn mehrere Buttons ähnlichen Text teilen (Sidebar-Toggle „Ladestationen ausblenden" vs. Karten-Legende mit Zähler) — im Zweifel `.leaflet-marker-icon`-Zählung nutzen, das ist eindeutig.
  - **v3.51.6 (Routenplaner: manuelle Pausen-Eingabe — drittes Ergebnis des Routenplaner-Wunschzettels, S10):** Neue Sektion „Pausen" in `RoutePlanner.vue` — Bezeichnung (optional) + Dauer in Minuten, hinzufügen/entfernen. Fließt in `timingResult` (Ankunfts-/Abfahrtszeit) und `downloadICS()` ein: `totalMin = duration_min + totalChargeMins + totalBreakMins`. Rein clientseitiger State (`breaks` ref), bewusst NICHT mit `/saved-routes` persistiert (im Unterschied zu `waypoints`) — Kernwunsch war die Zeitrechnung, keine dauerhafte Pausen-Verwaltung. **Live verifiziert:** Route Stuttgart→Ulm, Abfahrt 09:00 → ohne Pause X Uhr Ankunft, mit 45-min-Pause verschiebt sich „Voraussichtliche Ankunft" exakt um 45 Minuten (10:55), Pausen-Badge „+45 min" erscheint neben der bestehenden Ladezeit-Anzeige. i18n ×7 neue Keys.
  - **v3.51.5 (Routenplaner: Lademengen-Forecast kWh — zweites Ergebnis des Routenplaner-Wunschzettels, S10):** `planChargingStops()` (routing.js) berechnete `kwh2add` je Stopp schon immer (Grundlage der Ladedauer-Schätzung), gab es aber nie zurück — UI zeigte nur die SoC-Differenz, nicht die Energiemenge. Neu: `stops[].kwh_added` + `total_kwh_added` (Routensumme), im Frontend neben Ladedauer/Leistung + als Summenzeile. i18n ×7 (`routes.kwhAdded`/`totalKwhAdded`, eigener Namespace — **Merke:** `charging.kwhAdded` existierte schon seit v3.50.0 mit Parameter `{value}`, mein neuer `routes.kwhAdded` nutzt bewusst `{kwh}` in einem anderen Namespace, keine Kollision, aber beim Einfügen per Skript den richtigen Namespace/Anker prüfen — ein Ankerpunkt, der nur im PARALLELEN Feature-Branch existiert [hier: `routes.legs` aus PR #251], existiert im eigenen Branch (von main gezweigt) nicht, das Einfüge-Skript läuft dann still folgenlos durch ohne Fehler zu werfen.).
  - **v3.51.4 (Routenplaner: Gesamtreisezeit + Teilstrecken — erstes Ergebnis des Routenplaner-Wunschzettels von Sven, S10):** `RoutePlanner.vue` berechnete Fahrzeit (`routeData.duration_min`) und Ladezeit je Stopp (`stop.charge_minutes`) schon vorher, zeigte aber weder die kombinierte Gesamtreisezeit noch eine Teilstrecken-Aufschlüsselung. Neu: „Gesamtreisezeit"-Zeile (nur bei vorhandenem Ladeplan) + „Teilstrecken"-Liste (Start→WP1, WP1→WP2, …, WPn→Ziel) mit Distanz/Dauer je Segment, nur bei ≥ 1 Zwischenstopp sichtbar. **Backend-Fund:** `fetchValhalla()` (Umgehungsoptionen) fasste alle Legs zu einer Geometrie zusammen und verwarf `duration`/`distance` je Segment — Valhalla liefert das selbst (`data.trip.legs[].summary`), OSRM lieferte `legs` schon immer nativ mit. Beide Pfade liefern jetzt `legs` im selben Format. i18n ×7 neue Keys. **Live gegen echtes OSRM verifiziert** (Stuttgart → Geislingen → Ulm: 2 Teilstrecken 59km/53min + 32km/34min = 90km/1h27min, exakt deckungsgleich mit Gesamtstrecke). **S10-Roadmap (Routenplaner, priorisiert von Sven):** 1) ✅ Gesamtfahrzeit + Teilstrecken, 2) ✅ Ladedauer-Forecast/kWh, 3) ✅ Pausen-Eingabe, 4) ✅ Anbieter-Filter, 5) ✅ Community-Kommentare — alle 5 Punkte als eigene PRs geliefert (#251–#255).
  - **v3.51.3 (Dienstwagen-Steuer nur noch BEV):** Sven-Klärung zu `/dienstwagen-steuer`: der Rechner soll ausschließlich reine E-Fahrzeuge abdecken. Fahrzeugtyp-Selector (BEV/PHEV/Verbrenner) + PHEV-Eingaben (E-Reichweite, CO₂) aus `CompanyCarTax.vue` entfernt, `determineFactor()` in `companyCarTax.js` auf die BEV-Schwellenlogik reduziert (kein `vehicleType`-Parameter mehr), tote `phevRangeRequirement()`-Hilfsfunktion entfernt. Keine Tests im Repo zu diesem Modul vorhanden (die „X Assertions"-Angaben in früheren Einträgen waren Ad-hoc-Verifikationsskripte einzelner Sessions, nie Teil des Repos — `frontend/package.json`/`backend/package.json` haben kein `test`-Script).
  - **v3.51.2 (Fix Verbrauchsanzeige — „0.0 kWh/100km" statt „–" bei Fahrten ohne Fahrzeugdaten):** Sven-Report „fehlen immer Angaben" bei einer 3,5-km-Fahrt mit SoC „–% → –%". Ursache in `Trips.vue`/`Dashboard.vue`/`TripDetail.vue`: Verbrauchsberechnung prüfte nur `distance_km`, nicht `energy_used_kwh` — `null / distance_km * 100` ergibt in JS `0`, also ein irreführender Wert statt ehrlichem „–". **Kein Datenerfassungs-Bug:** `owntracks.js` schreibt beim Abschluss einer GPS-only-Fahrt (Smartphone-Tracking, kein Tesla-Fahrzeugkontakt) bewusst weder `energy_used_kwh` noch `start_/end_soc` (verifiziert: `UPDATE trips SET end_time=?, end_lat=?, end_lon=?, end_odometer_km=?, distance_km=?` — keine der drei Spalten wird gesetzt, bleiben NULL). Seit Teslas API-Sperre Mai/Juni 2026 läuft ein Teil des Trackings nur noch über diesen GPS-Pfad, daher trat der Anzeigefehler „immer" auf. `/fahrtwerte` hatte den Guard schon korrekt (Backend `trips.js`: `energy_used_kwh != null && distance_km > 0`); jetzt an allen drei Frontend-Stellen nachgezogen (`(trip.energy_used_kwh != null && trip.distance_km) ? ... : '–'`). **Diagnose-Weg:** Backend-Grep auf alle Schreibstellen von `energy_used_kwh` (bestätigt NULL statt 0 in der DB), Test-Trip mit `source='owntracks'` in lokale Demo-DB injiziert, Playwright bestätigt „–" bei der Test-Fahrt und unveränderte echte Werte bei den übrigen 49 Fahrten (keine Regression). **Merke:** `SELECT id FROM vehicles LIMIT 1` ohne `ORDER BY` ist beim Testdaten-Injizieren keine verlässliche Wahl des „richtigen" Fahrzeugs — Demo-Tenants können mehrere Fahrzeuge haben, das Frontend filtert nach `appStore.selectedVehicle`.
  - **v3.51.1 (Fix Fahrtwerte-Tabelle — fehlende Spalten waren nur ungescrollt):** Sven-Report „man kann nicht alle Spalten sehen" auf `/fahrtwerte`. `TripMetrics.vue` hat 12 Spalten und ist auf den meisten Bildschirmen breiter als der Viewport (**gemessen: 179 px Überhang bei 1440 px**) — kein Backend-Bug, keine fehlenden SQL-Felder, kein `overflow:hidden`-Blocker im Ancestor-Chain. Ursache: macOS/iOS blenden die Scrollbar standardmäßig aus, es gab keinen visuellen Hinweis auf weitere Spalten rechts. Fix: rechter Fade-Indikator (`canScrollRight`, ResizeObserver auf Scroller+Table, verschwindet automatisch beim Durchscrollen) + sticky erste Spalte (Datum) ab `sm:` (≥ 640 px) für Zeilenkontext beim Scrollen. **Nicht** auf Handy-Breite gepinnt: gemessen bei 390 px belegten gepinnte Spalte + Fade zusammen 42,5 % des Viewports — dort löst der Fade allein die Discoverability. **Keine Spalte versteckt** — das hätte die Beschwerde verschlimmert. **Diagnose-Weg:** lokaler Demo-Tenant + Playwright (Memory `project-tcv-local-repro`), DOM-Messung von `scrollWidth`/`clientWidth` vor und nach Fix, plus Sticky-Halt-Test (`getBoundingClientRect().left` vor/nach Scroll) und Gradient-Render-Check (`backgroundImage` enthält `rgb(15, 17, 21)` = `tesla-dark`) — beide bestätigt live im Chrome-Headless. **Merke:** Ein anderer automatisierter Prozess (Dependency-/Security-Autofix-Cron) arbeitet parallel im lokalen Checkout `~/GIT-Lokal/Tesla-Carview` und wechselt dort eigenständig Branches/committed/pusht — ein `git reset --hard` dieses Prozesses hat einen ersten Anlauf mit uncommitted Changes gekostet. Ab sofort: Frontend-/UI-Fixes an diesem Repo in einem `git worktree` bauen (node_modules symlinken statt neu installieren), sofort committen statt lange uncommitted zu lassen.
  - **v3.51.0 (Raspberry Pi 4 ist Mindestanforderung):** Pi 3 und älter (32-Bit-ARM) werden nicht mehr unterstützt — **Node.js veröffentlicht ab Major 24 keine ARMv7-Images**, weder alpine noch Debian, seit node:26 bricht `buildx --platform linux/arm/v7` mit „no match for platform in manifest" ab. `deploy/setup.sh` fängt `armv6l`/`armv7l`/`armhf` jetzt **vor** allen Installationsschritten ab und erklärt die Lage, statt erst beim Image-Pull zu scheitern. Doku-Sync vollständig: README ×7, Handbuch ×6, `docs/02-deployment.*` ×7 (Tabelle + Fußnote), `docs/15-raspberry-pi-storage.*` ×7, Wiki ×7 (Installation + Storage). **Merke:** Ein Pi 4 kann 32-Bit-OS fahren — `uname -m` entscheidet (`aarch64` gut, `armv7l` nicht). Rückweg für 32-Bit-ARM wäre `node:22` (letzte Major mit ARMv7), ein Fork-Pfad ohne Node-Security-Updates.
  - **v3.50.2 (Alarm bei Drift):** Der wöchentliche `schedule`-Lauf des Build Gates erkannte Basisimages, die unter gleichem Tag mit neuem Inhalt veröffentlicht wurden — aber ein rotes Ergebnis erreichte niemanden (kein PR hängt daran). Jetzt öffnet ein roter Lauf ein Sammel-Issue (Label `security`), das bei grünem Lauf automatisch geschlossen wird. Bewusst ein Issue statt eines externen Dienstes: braucht nur `GITHUB_TOKEN`, es muss kein Secret zwischen Systemen bewegt werden. Telegram ist vorbereitet, aber inaktiv ohne `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID`. **Merke (v3.50.1+):** `security-autofix.yml` dispatcht die Required Checks selbst — die Liste dort muss jeden neuen Required Check enthalten, sonst bleibt er auf „Expected" und der Autofix-PR ist nie mergebar (behoben in #243).
  - **v3.50.1 (Docker Build Gate + zwei Funde daraus):** Neuer Workflow `docker-build-gate.yml` — baut beide Images **während des PRs** und scannt das fertige Laufzeit-Image (`trivy image`, `scanners: vuln,secret`). Schließt die Lücke, dass `deploy.yml` an CI-auf-`main` hängt: Ein PR mit getauschtem Base-Image war vorher ungetestet mergebar. **Zwei Jobs:** `build + trivy` (amd64, läuft immer → Required Check) und `multi-arch build` (amd64+arm64, teure QEMU-Emulation, Steps hängen an einem `changes`-Job). Beide sind Required Checks — ein Job mit `paths`-Filter oder Job-`if` meldet keinen bzw. nur „skipped" und taugt dafür nicht. **Merke — Scan-Ziel entscheidet:** `trivy fs` (security.yml) sieht Lockfiles, nicht den Laufzeit-Stand; bei Multi-Stage meldet es CVEs aus Builder-Stages, die nie laufen (ins `nginx:alpine` wandert nur `/app/dist`). Genau daran hing die These „node:26-bookworm-slim hat 6 CRIT, wechsle auf alpine": Gemessen liefern node:26-bookworm-slim, node:26-alpine, node:24-alpine und node:26-trixie-slim **dieselben 4 behebbaren Findings** — Node-eigene gebündelte Pakete, distro-unabhängig. Der Wechsel hätte 0 CVEs behoben.
    - **Fund 1 — npm aus dem Backend-Image entfernt:** `CVE-2026-59873` (CRITICAL, `tar` → 7.5.19) saß im mit Node gebündelten npm (`/usr/local/lib/node_modules/npm`), das eigene Pakete mitbringt (tar, undici, brace-expansion). **Kein Base-Image behebt das** (node:24/26-alpine liefern tar 7.5.16). Container startet mit `node src/index.js`, npm wird zur Laufzeit nicht gebraucht → nach dem Install entfernt. Nebeneffekt: kein Paketmanager mehr für einen Angreifer mit Code-Ausführung.
    - **Fund 2 — arm/v7 (Pi 3) ist tot:** **Node veröffentlicht ab Major 24 keine arm/v7-Images**, weder alpine noch Debian. Mit dem Wechsel auf node:26 ist der Pi-3-Build faktisch entfallen (`buildx --platform linux/arm/v7` → „no match for platform in manifest"), obwohl beide Dockerfiles ihn zusicherten. Kommentare korrigiert, Gate baut amd64+arm64. Die frühere glibc-Begründung im Frontend (rolldown ohne musl-arm/v7-Binding) ist damit gegenstandslos — `bookworm-slim` bleibt trotzdem, weil ein alpine-Wechsel messbar nichts bringt. **Offen als Produktentscheidung:** Pi 3 zurückholen hieße node:22 (letzte Major mit arm/v7); README ×7, Wiki und Handbuch behaupten weiterhin Pi-3-Support und sind bewusst unangetastet geblieben.
  - **v3.50.0 (Feature Ladepreis-Ranking — drittes Ergebnis der TFF-Forum-Analyse):** Neue Sektion in `Charging.vue` + `services/chargingPriceRanking.js` + `GET /api/charging/price-ranking`. Sortiert Ladeorte nach **€/kWh** — bewusst NICHT dasselbe wie das bestehende `/cost-by-location` (das zeigt, wo das meiste Geld hinfließt, typischerweise zuhause wegen der Menge). Aufpreis je Ort gegen den energiegewichteten Heimpreis + Gesamtsumme. **Heim-Definition wiederverwendet aus `billing.js`** (`is_home_charged` → `charging_locations.type='home'` → Heuristik über `charger_type`), keine zweite eingeführt. **Datenehrlichkeit:** Ladungen ohne `cost` sind UNBEKANNT, nicht kostenlos — sie bleiben aus der Preisrechnung raus (sonst sähe jeder Ort künstlich billig aus), Abdeckung wird ausgewiesen, unter 50 % bepreister Energie keine Gesamtaussage. `is_free=1` zählt dagegen als echte 0, ist aber aus der Heim-Referenz ausgenommen (sonst zöge eine geschenkte kWh den Referenzpreis). Orte < 5 kWh fliegen raus. **Merke:** `restrictToOwnVehicles(req, spalte)` nimmt den Spaltennamen als Parameter — bei Queries mit JOIN `'cs.vehicle_id'` übergeben, NICHT das Fragment per Regex nachbearbeiten (das trifft auch das `vehicle_id` in der Subquery → kaputtes SQL). i18n ×7, Handbuch DE/EN, README ×7. 46 Assertions (30 Logik + 16 Endpoint inkl. Nicht-Admin-Zugriffsbeschränkung).
  - **v3.49.0 (Feature Energiebilanz „Was verbrauchst du wirklich?" — zweites Ergebnis der TFF-Forum-Analyse):** Neue Sektion in `EnergyReport.vue` + `services/energyBalance.js` + `GET /api/energy/balance`. Beantwortet die häufigste Verbrauchsfrage der Community: Tesla zeigt nur Fahrtverbrauch, Standby/Wächter/Ladeverluste fehlen dort. **Als LEITER gebaut, nicht als eine Gesamtbilanz** — (1) Fahrt `energy_used/km` exakt, (2) Akku-zu-Rad `(geladen − Δsoc·Kapazität)/km` exakt (enthält Standby IMPLIZIT), (3) Netz-zu-Rad = Sprosse 2 ÷ Ladewirkungsgrad (v3.48.0), als Schätzung gelabelt. Fällt eine Quelle aus, endet die Leiter früher statt zu raten. **Absicherung ist die Bilanzgleichung selbst:** `geladen ≈ Fahrt + Standby + Δsoc + Residuum`; Residuum > 15 % des Durchsatzes → keine Zahl, sondern Grund im Klartext (`residual_too_large`). Kapazität wird aus Ladungen mit SoC-Hub ≥ 40 Punkten **gemessen** (Median, plausibel 0,4–1,2 kWh/%), Fallback 75 kWh — bewusst nur Hilfsgröße, die SoH-Aussage bleibt bei `Battery.vue`. SoC-Randwerte aus `battery_snapshots` (max. 1 Tag Abstand), Default-Fenster 90 Tage. **Standby-Zeile nur bei echter Abdeckung** (erstes Schlaf-Event in den ersten 20 % des Fensters) — sonst wäre „0,0 kWh" eine Behauptung, weil `vehicle_sleep_events` erst seit v3.47.1 gefüllt wird. i18n ×7, Handbuch DE/EN, README ×7. 48 Assertions (33 Logik + 15 Endpoint gegen echte SQLite).
  - **v3.48.0 (Feature Ladewirkungsgrad — erstes Ergebnis der TFF-Forum-Analyse):** Neue Sektion in `Charging.vue` + `services/chargingEfficiency.js` + `GET /api/charging/efficiency`. **Kern-Erkenntnis:** `charging_points.power_kw` ist `charge.charger_power` und damit die **NETZSEITE**, `energy_added_kwh` der kumulative Akku-Zähler — der Wirkungsgrad lässt sich also für ALLE Nutzer rechnen, nicht nur für Monta-Kunden mit MID-Zähler. Zwei Methoden getrennt ausgewiesen (`metered` = `energy_kwh_mid`, exakt; `telemetry` = geschätzt), nie vermischt. **Gerechnet wird intervallweise** (Netz-Trapez über `power_kw` gegen Akku-Delta aus dem kumulativen Zähler) — NICHT Session-Summe gegen Session-Summe: Der erste Messpunkt fällt auf den Poll nach Ladestart, der letzte auf den davor, über die Session summiert fehlte die Netz-Energie der Ränder → systematisch > 100 %. Ablehnungskriterien statt Schätzung: < 2 Punkte, < 3 kWh erfasst, Lücke > 70 min, Ergebnis außerhalb 60–100 %. Aufschlüsselung nach Leistungsband (`POWER_BANDS`), energiegewichtet. Ein Query für alle Messpunkte (kein N+1). i18n ×7, Handbuch DE/EN, README ×7. 41 Assertions (28 Logik + 13 Endpoint gegen echte SQLite). **Merke:** `fleetTelemetry.js` schreibt KEINE `charging_points` — Ladedaten kommen ausschließlich vom Poller, bei aktiver Telemetrie also nur stündlich. Für AC/Heimladen (konstante Leistung) reicht das, DC-Schnellladungen bleiben meist unbewertet.
  - **v3.47.1 (Fix Schlaf-Monitor — schrieb noch nie einen Event):** Zwei Ursachen. (1) Der Online-Status wurde aus `vehicle_state.state` gelesen — **das Feld existiert in Teslas Antwort nicht**, der Status steht top-level in `state` (poller.js nutzte das an anderer Stelle schon korrekt). (2) Die eigentliche: **ein schlafendes Fahrzeug beantwortet `vehicle_data` gar nicht** (HTTP 408, im Poller geschluckt) — der Übergang „wach → schlafend" ist über diesen Endpoint prinzipiell unbeobachtbar. Dazu hing `trackSleepEvents` nur an `syncVehicleState`, das der Haupt-Loop bei aktiver Fleet-Telemetry überspringt (`isCoveredByTelemetry`). **Lösung:** Schlafstatus aus dem **Fahrzeug-LIST-Endpoint** (`GET /api/1/vehicles` → `state`, weckt das Auto nicht), eigener Loop `startSleepMonitor()` in poller.js, bewusst entkoppelt vom vehicle_data-Takt (der darf nicht schrumpfen, sonst reißt `TESLA_DAILY_CAP`). Default 15 min via `TESLA_SLEEP_POLL_MINUTES`, ein Listen-Call pro Mandant; zählt nicht gegen die Caps (die filtern `%/vehicle_data`), wird aber in `tesla_api_usage` getrackt. **Zwei Folgefehler mit:** Zustand jetzt DB-verankert (offener Event = galt zuletzt als schlafend) statt Prozess-Map — überlebt Container-Neustarts, die bei 90-min-Idle ganze Schlafphasen verschluckten; Wake-SoC wird aus dem ersten `battery_snapshot` nach `wake_at` nachgetragen statt aus dem nächstbesten Sync (bei Telemetry kommt der erst mit dem Stunden-Heartbeat, dann steckt Fahrverbrauch drin). Ø-Verlust in `routes/sleep.js` nur aus Phasen ≥ 60 min und ohne negative Werte (Tunnel-Flaps ergäben sonst 24 %/h). 26 Assertions gegen In-Memory-SQLite. **Offen: Live-Verifikation** — dass der LIST-Endpoint je Fahrzeug wirklich `state` liefert, war ohne Prod-Zugang nicht prüfbar (SSH Port 22 refused, Browser-Extension nicht verbunden). Der Code fällt sauber aus, wenn das Feld fehlt → dann täte der Fix stillschweigend nichts. **Sven prüft nach dem Deploy, ob binnen eines Tages Events auf `/sleep` erscheinen.**
  - **v3.47.0 (Feature Manipulationssicheres Fahrtenbuch, S09 — Sven wählte „Hash-Chain je Änderung + HMAC-signiert"):** `services/tripLedger.js` — append-only `trip_ledger`, jede Fahrt-Änderung schreibt einen HMAC-signierten Eintrag (`prev_hash` verkettet auf Vorgänger-HMAC), Signaturschlüssel `data/.ledger-key`. Zentral über `logChanges` (tripAudit.js) eingehängt + explizit bei create/delete. `GET /api/trips/ledger/verify` (rechnet Kette nach) + `GET /api/trips/:id/ledger`. Migration legt Tabelle an + Genesis-Backfill (Marker `ledger.genesis_done`). `Fahrtenbuch.vue`: Integritäts-Badge + Aussage im Finanzamt-PDF (DE-only View). **Live verifiziert:** Snapshot-Manipulation → `content_hash`-Bruch, Eintrag gelöscht → `prev_hash`-Bruch. **Damit S09 komplett** (Versteuerung + SoH + Ledger).
  - **v3.46.0 (Feature SoH-Zertifikat, S09):** Batterie-Ansicht („Prognose") erzeugt ein **Batterie-Gesundheitszertifikat als PDF** (`exportSohCertificate` in `Battery.vue`, jsPDF+autoTable). Nutzt das bestehende `/api/battery/forecast` (kein neues Backend): aktuelle Reichweite @100 % (`current_soh_km`), Degradationsrate %/a, R², Datenbasis, 3-Jahres-Prognose, Zeit bis 80 %. Optionale WLTP-Neu-Reichweite (localStorage) → SoH in %. Klarer Gewährleistungs-Ausschluss. i18n ×7 (`battery.sohCert`, 25 Keys). Merke: keine Werks-Reichweite je Modell gespeichert → SoH-% nur mit User-Eingabe.

  - **v3.45.0 (Feature Dienstwagen-Versteuerungs-Assistent, S09):** neue View `CompanyCarTax.vue` (`/dienstwagen-steuer`, Nav „Auswertungen", nur `category=company`) + reine Rechenlogik in `frontend/src/lib/companyCarTax.js` (unit-getestet). Vergleicht 1-%-Regel vs. Fahrtenbuchmethode. **E-Satz ist datumsabhängig** (Sven-Vorgabe): `determineFactor` wählt 0,25/0,5/1 % nach Fahrzeugtyp + BLP + Anschaffungsdatum; BEV-Viertelungs-Grenze 60.000 € (bis 2023) / 70.000 € (ab 2024) / 100.000 € (ab 01.07.2025), Sonderregel bis Ende 2030; PHEV 0,5 % nur bei ≤50 g/km oder E-Reichweite ≥40/60/80 km je Jahr. Reduktion wirkt auch auf 0,03-%-Pendlerzuschlag + (Fahrtenbuch) AfA. Kosten aus `/tco/vehicles/:id`, Privatanteil aus `/trips/stats`. Rein clientseitig, DE-only, Orientierungsrechnung. Regeln 2026-07 gegen ADAC/Haufe verifiziert. **Offen S09:** SoH-Zertifikat-PDF, manipulationssicheres Fahrtenbuch.
  - **v3.44.0 (Feature PV-Überschussladen, S08):** neue View `PvSolar.vue` (`/pv-solar`, Nav „Planung") + `homeAssistantService.js` (`readEntityState` liest HA-Sensor via REST, `surplusToAmps`) + Route `pv.js` (`GET/PUT /api/pv/config` [Admin, Token redacted], `GET /api/pv/status`, `POST /api/pv/:vehicleId/apply`). Empfehlung = `Überschuss ÷ (Spannung × Phasen)`, gedeckelt auf Max-Strom, unter 5 A = nicht ladbar. „Anwenden" sendet `set_charging_amps` + `charge_start`/`charge_stop` via `apiProxyPost` (Fleet + Virtual Key). Config in `tenant_settings` (configService). Rein lokal, kein Cloud-Zwang. Live gegen Mock-HA verifiziert (3200 W → 13 A, Schwellen-Logik korrekt). i18n ×7, Handbuch 6 Sprachen, README ×7. **Offen (v2):** kontinuierlicher Regelkreis (Scheduler statt manuellem „Anwenden") — braucht Svens Live-HA + Auto zum Tunen. **Damit S08 komplett** (Ladeplaner + Kostensplit + PV).
  - **v3.43.0 (Feature Dienstlich/Privat-Kostensplit, S08):** neue Sektion „Dienstlich / Privat — Erstattung" in `Kostenabrechnung.vue` (nur `category=company`) + Backend `GET /api/billing/:vehicleId/reimbursement` in `billing.js` (Heimladekosten + km nach `trip_type` je Jahr/Monat, UTC-Fenster wie die Summary). Zwei Methoden: **Pauschale** (§ 3 Nr. 50 EStG, 30/70 € × Monate, client-berechnet) und **Fahranteil** (dienstlich = business+commute ÷ gesamt × Heimladekosten). Erstattungs-PDF je Methode via jsPDF+autoTable. **Bewusst deutschsprachig** (kein i18n — wie die gesamte Kostenabrechnung, deutsches Steuerthema); CHANGELOG DE/EN, Handbuch DE/EN, README ×7. Live gegen injizierte Testdaten verifiziert (400/600 km = 67 %, 21 € Heim → 14 € Erstattung). Merke: Dienstwagen-Gate ist `category==='company'` (nicht 'business').
  - **v3.42.0 (Feature Ladeplaner, S08):** neue View `ChargePlanner.vue` (`/ladeplan`, Nav-Gruppe „Planung") + Backend `planCharge()` in `tariffService.js` und `GET /api/tariff/charge-plan`. Greedy wählt die günstigsten — auch nicht zusammenhängenden — Stundenslots bis zur Abfahrt aus der aWattar/Tibber-Preiskurve (Rand-Slots anteilig); vier Kennzahlen (Nachladen inkl. erreichtem SoC / Ladedauer / optimale Kosten / Ersparnis gegen „sofort") + Balken mit grün markierten Slots; Ladeverluste via `efficiency` (AC ~0,9). **Reine Preiskurven-Rechnung, KEIN Tesla-/Fleet-Call.** i18n ×7 (`chargePlanner`, `nav.chargePlanner`), Handbuch DE/EN/ES/FR/TR/EL, README ×7. Merke: NavBar nutzt hardcoded `label` (`it.label ?? t(...)`); der i18n-Key `nav.<key>` greift nur in SettingsWizard/MobileTabBar. **Offen für S08:** PV-Überschussladen (EVCC/HA/Modbus) und dienstlich/privat-Kostensplit-PDF sind noch nicht gebaut.
  - **v3.41.5:** CI-Ausbau nach fleet/Skillmatrix-Muster: (1) Auto-Rebase-Fix — `dependabot-auto-merge.yml` zieht bei jedem main-Push alle offenen Dependabot-PRs per `gh pr update-branch` nach (vorher blieben Bot-PRs „behind" hängen). (2) Neuer Workflow `security-autofix.yml`: täglich `npm audit fix` (semver-kompatibel) über backend+frontend → idempotenter PR auf `chore/security-autofix`, plus trivy-CVE-Sammel-Issue (schließt sich bei 0 Findings). Required-Checks werden nach dem Bot-Push per `workflow_dispatch` angestoßen (GITHUB_TOKEN-Rekursionsschutz), dafür hat ci.yml jetzt einen dispatch-Trigger.
  - **v3.41.4:** Zoom-Drossel Teil 2 — der Live-429 kam NICHT nur von express, sondern (auf iland) vom **Host-nginx** (`deploy/nginx-host.conf.template`: zone=api 120r/m burst=20 auf `location /api/` inkl. Tiles). Template hat jetzt eigene zone=tiles (1200r/m, burst=300) + `location /api/tiles/`. **Host-nginx auf iland muss manuell nachgezogen werden** (Template anwenden + reload) — Container-Deploy ändert die Host-Config nicht. Merke: 429-Quelle unterscheiden via `ratelimit-*`-Header (express) vs. nackte nginx-Antwort.
  - **v3.41.3:** Karten-Zoom fraß das API-Rate-Limit leer (Sven-Report: „nach Map-Zoom muss ich die Seite neu laden um Menüpunkte auszuwählen"). `/api/tiles` zählte gegen `apiRateLimit` (120/min/IP); ein Zoom = 50–150 Kacheln → Rest der Minute 429 auf ALLE API-Calls. Fix: eigener `tileRateLimit` (1200/min/IP) + `skip` für `/api/tiles` im allgemeinen Limiter (`security.js`, `index.js`). Diagnose-Weg: lokal Demo-Tenant + Playwright; 150 parallele Tile-Requests → `/api/health` 429 reproduziert.
  - **v3.41.2:** Einmalige mph-Korrektur-Migration für Telemetrie-Punkte vor v3.35.3 (Marker `migration.telemetry_speed_mph_fix` in tenant_settings, Cutoff 2026-07-03T19:25:45Z).
  - **v3.41.1:** Nav thematisch statt alphabetisch (Sven-Wahl per Preview): `GROUP_ORDER` in nav.js, `NAV_DEFAULTS_VERSION=3` (einmaliger Order-Reset). Erster Release über den neuen PR-Workflow.
  - **v3.41.0:** Heatmap-Ebenen-Farben anpassbar (Color-Picker je Layer, localStorage, Reset-Button). Tile-Leere Runde 2: Cache-Default nach `data/tiles` (persistent), Upstream-Retry über Mirror-Rotation, Frontend-Tile-Retry via gemeinsamem `lib/tiles.js#osmTileLayer` (alle 5 Karten). NavBar-Dropdowns hinter Karte gefixt: `isolate` auf allen Karten-Containern.
  - **v3.40.0 (Sammel-Release):** (1) Heatmap-Layer „Fahrwege" — `GET /api/trips/route-lines` (max. 300 Trips, 60 Punkte/Trip downsampled), lazy im Frontend. (2) App-Hub-CRUD für Admins — Tabelle `launcher_custom_apps`, POST/PUT/DELETE `/api/launcher/admin/apps`, Custom-Apps mit Klartext-`label` (kein i18n-Key; Frontend-Fallback `label_i18n ? $t(...) : label`). (3) Fahrtwerte-Limit „Alle" (`limit=0` → SQLite `LIMIT -1`). (4) Nav alphabetisch je Gruppe (programmatischer Sort in nav.js, `NAV_DEFAULTS_VERSION=2` resettet gespeicherte Order einmalig, hidden bleibt). (5) Tile-Proxy: In-flight-Dedupe + max. 8 parallele OSM-Fetches + Stale-Fallback; `TILE_CACHE_DIR`-Env für persistentes Volume.
  - **Feature Zonen-Analyse im Fahrtdetail (v3.39.0):** Karte „Zonen-Analyse" in `TripDetail.vue` — 3 Modi (Tempo-Zonen / Meine Zonen [Geofences+Ladeorte] / Abschnitt via Von-Bis-Regler), tabellarische Werte + Karten-Hervorhebung (Segment-Refs aus initMap, `applyMapOverlay()`); Checkbox „📍 Hinweise" (Vmax/Max-Leistung/Max-Reku/Stopps ≥ 1 min). Alles clientseitig aus `trip.points` (dt-Cap 120 s, Energie netto). i18n ×7 (`tripDetail.zones`), Handbuch DE/EN (Rest im nächsten Doku-Sync).
  - **Fix Telemetrie-Erfassungskette (v3.38.4):** (1) `/api/trips/metrics` aggregierte nur `trip_points` → Fahrtwerte-Tabelle leer für Telemetrie-Fahrten; jetzt UNION ALL über `trip_points`+`telemetry_points`. (2) fleetTelemetry rief nie `geocodeTrip` → Koordinaten statt Adressen; jetzt Fire-and-forget-Hook bei Trip-Start/-Close (wie OwnTracks). (3) Gear-Datum ohne GPS → `start_/end_lat` NULL; Trip-Close zieht Start/Ziel aus Trackpunkten nach + idempotenter Daten-Repair in `runTenantMigrations`. Merke: `trip_points` = Poller/OwnTracks, `telemetry_points` = Fleet-Telemetry (Detail-Endpoint `/:id` switcht nach `t.source`).
  - **Fix Heatmap-Layer unsichtbar (v3.38.3):** `HeatmapMap.vue` + `LocationHeatmap.vue` zeichneten `L.circle` mit Meter-Radius (60–140 m) — bei auto-gefitteter Übersichtskarte (Zoom ≤ 11) subpixel-klein, Layer wirkten leer trotz korrekter Punktzähler. Jetzt `L.circleMarker` mit zoomunabhängigem Pixel-Radius (5–14 px, gewichtsabhängige Größe/Deckkraft).
  - **Webfonts self-hosted (v3.38.2, Privacy):** Bricolage/Manrope/JetBrains Mono als variable woff2 unter `frontend/public/fonts/` (SIL OFL, Subsets latin/-ext/cyrillic/-ext/greek/-ext), same-origin via `/fonts/fonts.css`; Google-`<link>`+preconnect aus `index.html` raus → keine Google-Requests mehr. CSP wieder strikt (`font-src 'self'`, helmet + `deploy/nginx-host.conf.template` zurückgesetzt). **Hinweis Live-iland:** die traefik-Middleware `sec-headers-app` erlaubt noch die Font-Hosts — harmlos (App fragt Google nicht mehr an), kann aber zurückgedreht werden. Font-Extraktion: `scripts`-losem curl-Skript, Details [[project_teslaview_csp_source]].
  - **Doku-Sync (v3.38.1):** Handbuch-Sektion „Fahrtwerte & Heatmap" ({#fahrtwerte-heatmap}) in allen 6 Handbüchern + Feature-Zeilen in README es/fr/tr/el/uk nachgezogen. Doku-Pflicht für v3.37/3.38 damit vollständig.
  - **Feature Geo-Heatmap (v3.38.0):** neue View `HeatmapMap.vue` (`/heatmap`) — Leaflet-Karte, 3 toggle-Layer (Fahrten rot / Ladevorgänge grün / Ladeorte blau). Rendering per gewichteten `L.circle` (kein Extra-Dep, wie das bisher ungenutzte `LocationHeatmap.vue`). Backend: neuer `GET /api/charging/location-heatmap` (charging_sessions ~100-m-Raster) + vorhandener `/api/trips/location-heatmap` + `/api/charging-locations`. Nav-Eintrag `heatmap`. i18n×7 (`heatmap`). Gitleaks-Falle: KEINE `key: '<langer_wert>'`-Muster (triggert generic-api-key) — Tabellen-Property hieß darum `field`.
  - **Feature Fahrtwerte-Tabelle (v3.37.0):** neue View `TripMetrics.vue` (`/fahrtwerte`), Backend `GET /api/trips/metrics` (EIN SQL-Query: trips LEFT JOIN GROUP-BY-Aggregat aus `trip_points` → min/max/Ø Speed+Leistung, kein N+1). Sortierbar, Summen-Kacheln, CSV (`;`+BOM), unit-aware (useUnits). Nav-Eintrag `fahrtwerte` + Button in Trips.vue. i18n×7 (`tripMetrics`).
  - **Fix `null.toFixed`-Render-Crash (v3.36.9):** ungeschützte `.toFixed()` auf leeren Feldern (`Battery.vue` Precondition-Temp, `TripsHeatmap.vue` km, `ChargingHeatmap.vue` Ø/Peak-kW) mit `?? 0` abgesichert.
  - **Fix Logout-bei-Reload — ECHTE Ursache: Router-Guard-vs-Restore-Race (v3.36.8, #14):** Serverseitig alles ok (Live-nginx-Log: `/auth/refresh` UND `/auth/me` beim Reload beide `200`), aber der Referer war schon `/login`. Grund: Router-Initial-Navigation lief parallel zur Session-Wiederherstellung; der `beforeEach`-Guard las `isAuthenticated`, während `refresh` lief aber `user` noch nicht gesetzt war → `false` → redirect `/login`; das nachziehende `200` kam zu spät. Fix: geteilte `ensureSessionRestored()` in `store/auth.js` — `main.js`-Boot UND Guard warten auf DIESELBE einmalige Restore-Promise (genau ein `/auth/refresh`, kein Rotations-Race). **Diagnose-Weg:** Container-nginx-Access-Log (`sudo docker logs tesla-carview-nginx`), nicht Host-nginx (leer). Die vorigen Anläufe v3.36.4 (`no-store`) und v3.36.6 (Service-Worker) waren Fehldiagnosen — bleiben als Härtung drin.
  - **CSP Google Fonts (v3.36.7):** Der ausgelieferte CSP-Header kommt vom **nginx** (`deploy/nginx-host.conf.template`), NICHT von helmet → helmet-Edit aus v3.36.6 war wirkungslos. Template erlaubt jetzt `fonts.googleapis.com`/`fonts.gstatic.com`. Wird nur via `setup.sh` angewendet → Live-Host braucht manuellen nginx-Reload. **Hinweis:** ausgeliefert wird über die Container (`tesla-carview-nginx`/`-proxy`), der Host-nginx-Site-Log ist leer — die maßgebliche nginx-Instanz ist noch zu verifizieren.
  - **Fix Safari-Logout-bei-Reload (v3.36.4, #14):** API-Antworten hatten kein `Cache-Control` (nur ETag) → Safari cachte `GET /auth/me` → gecachte Fehlantwort → Session-Restore scheiterte trotz erfolgreichem Refresh. Fix: `Cache-Control: no-store` für alle `/api` (außer `/api/tiles`) in `index.js`. Temp-Diagnose (`/api/auth/_diag`) wieder entfernt (auch Security-Finding erledigt). Diagnose-Weg war: HTTPS-Ringpuffer, weil SSH-Whitelist unzuverlässig.
  - **Geschwindigkeit mph/km/h wählbar (v3.36.0):** Speed folgt `unit_distance` (km→km/h, mi→mph). Neuer `fmtSpeed` in `useUnits` (prefs.js); umgestellt: TripDetail (Ø/Slider/Chart) + Telemetry Live-Speed. Interne Basis bleibt km/h.
  - **Fix Telemetrie-Speed mph→km/h (v3.35.3):** `VehicleSpeed` (mph) wurde ungerechnet als `speed_kmh` gespeichert → Schieber zeigte mph als km/h. Jetzt `× 1,60934` in `extractPoint`. Diag-Log aus v3.35.2 entfernt (Value-Typ-Fix bestätigt: SoC 742/Leistung 213 in 3h, vorher 0). Alte Telemetrie-Punkte behalten mph-Wert.
  - **Fix Telemetrie-Value-Typen (v3.35.2):** SoC/PackVoltage/PackCurrent kamen als int/string, Decoder las nur float → SoC & Leistung wurden NIE gespeichert (0/15k). `numVal` in `fleetTelemetry.js` liest alle Zahlentypen robust. **Verifiziert live.**
  - **Fix Fahrt-Detail GPS-Strecke/Slider (v3.35.1):** Telemetrie-Punkte sind sparse (Felder in eigenem Takt) — `GET /api/trips/:id` liefert jetzt kohärente Trackpunkte (LOCF für Speed/Leistung/SoC, nur Punkte mit lat/lon, gerundet). Route-Polylinie + Slider funktionieren mit Telemetrie-Fahrten. `trips.js` + `TripDetail.vue`.
  - **Live-Ladekurve (S07, v3.35.0, #174):** `GET /api/charging/current` (offene Session + Erwartungskurve aus vergleichbarer Session, nutzt vorhandene `charging_points` — kein Extra-Tesla-Call); `ChargingLive.vue` (30-s-Poll, Live-Power/SoC + Erwartungs-Overlay), eingehängt in `Charging.vue`.
  - **Fahrstil-/Effizienz-Score (S07, v3.34.0, #175):** `buildEcoScore` in `insightEngine.js` — relativer Effizienz-Index (Median letzte 30 T vs. eigener 180-T-Schnitt) + datenbelegte Tipps; Endpoint `GET /api/insights/eco-score`; Dashboard-Karte `eco_score` (Donut) in `Dashboard.vue`. Rein statistisch, lokal.
  - **Reifendruck-Trend & Slow-Leak-Warnung (S07, v3.33.0, #176):** TPMS-Zeitreihe (`tire_pressure_snapshots`, Poller-Write in `dataSync.js`) + temperaturbereinigte Trenderkennung in `companionEngine.js` (`findTireAnomalies`, stabiler Wochen-Hash gegen Alarm-Spam) → Warnung über die bestehenden Kanäle; `TireMap.vue` zeigt Warn-Ring/Badge + Trend-Tooltip; Endpoint `GET /api/telemetry/:id/tire-history`.
  - **Setup-Wizard-Fix (#170):** `db.transaction()`-Callback in `/api/setup/init` synchron gemacht (better-sqlite3 lehnt async-Callbacks ab).
  - **Fleet-Telemetry Ende-zu-Ende funktionsfähig (v3.32.4/.5):** Receiver entpackt FlatBuffers-Envelope + eingebettetes Protobuf; `fleet_telemetry_config` über den Fleet-Level-Endpoint mit echter CA-Kette. Live verifiziert.
  - **Betriebs-Selbsttest (v3.32.0):** `selfCheck.js` + `GET/POST /api/system/self-check`.
- **Doku-Stand:** Ladeplaner (v3.42.0), Kostensplit (v3.43.0), PV-Überschuss (v3.44.0): App-Doku (README ×7, Handbuch) + **Wiki (7 Sprachen, gepusht)** synchron; Marketing-Site `teslaview-web` bis v3.44.0 gepflegt (PR #5 offen). **Dienstwagen-Steuer (v3.45.0/v3.51.3 BEV-only):** README ×7 + Handbuch DE/EN + Wiki DE/EN/UK — bewusst nur DE/EN/UK, deutsches Steuerthema. **SoH-Zertifikat (v3.46.0):** README ×7 + Handbuch DE/EN + i18n ×7. **Manipulationssicheres Fahrtenbuch (v3.47.0):** README ×7 + Handbuch DE/EN (DE-only View). **Routenplaner-Wunschzettel (v3.51.4–.9):** README ×7 + Handbuch ×6 + Wiki ×7 synchron (v3.51.9). **Reverse-Proxy-Rate-Limits (v3.52.4/v3.52.5):** `docs/02-deployment.*` ×7 + Handbuch ×6 + `deploy/README.md` + `deploy/traefik-dynamic.example.yml` + Setup-Ausgabe + Wiki (Installation/Troubleshooting) synchron. **Offen:** Wiki für v3.46.0/v3.47.0; Marketing-Site `teslaview-web` für v3.45.0–v3.51.9. **Regel (Sven): Doku + Marketing-Site immer im selben Arbeitsgang wie das Feature — nie auf einen Sammel-Sync verschieben.**

### Architektur-Ankerpunkte (stabil — hier nachschlagen)

- **DB-vor-env-Pattern:** `backend/src/services/configService.js` → `getTenantSetting(db, key, envFallbackKey)` liest `tenant_settings` (SQLite), Fallback `.env`.
- **Fleet-Telemetry-Receiver:** `backend/src/services/fleetTelemetry.js` (FlatBuffers-Envelope → eingebettetes Protobuf).
- **Fahrzeug-Status vs. `vehicle_data` (Fallstrick):** Der Online-Status (`online`/`asleep`/`offline`) steht **top-level** in der `vehicle_data`-Antwort (`state`) — nicht in `vehicle_state`. Wichtiger: **ein schlafendes Auto beantwortet `vehicle_data` überhaupt nicht** (HTTP 408). Alles, was den Schlafzustand braucht, muss den **LIST-Endpoint** (`GET /api/1/vehicles` → `state`) nutzen; der weckt das Auto nicht und läuft auch bei aktiver Fleet-Telemetry. Siehe `startSleepMonitor()` in `poller.js`.
- **Betriebs-Selbsttest:** `backend/src/services/selfCheck.js`.
- **Version dynamisch:** Backend liefert sie über `/api/system/version` aus der package.json; Frontend zeigt sie in `AppFooter.vue`/`System.vue` — niemals hart kodieren.
- **Admin-UI-Config-Routen:** `GET/PUT /api/system/{tesla-credentials,vapid-config,telegram-config,abrp-config}`, `POST /api/system/vapid-generate`, `PUT /api/grok/config`.

### Offen / Ideen

- Neue Feature-Roadmap nach v3.32 (siehe Issues). Die Value-Drop-Roadmap S01–S06 ist abgeschlossen.

### Deployment

Dokploy deployt die App automatisch nach jedem Merge auf `main`. Die Marketing-Site `teslaview-web` deployt per SSH-Pull auf den Host (GitHub Action, kein Build) beim Merge auf deren `main`. Für manuelle Eingriffe: Zugang und Repo-Pfad sind in der privaten Deployment-Dokumentation hinterlegt.

---

## Was bei Verdacht auf bereits geleaktes Geheimnis tun

1. **Sofort rotieren** — Token, Key, Password in der Quelle ändern
2. **NICHT** `git rm` + commit — das Original bleibt in der Historie
3. `gh secret-scanning ...` bzw. GitHub-UI nutzen, um den Alert
   zu öffnen
4. Falls History-Rewrite nötig: `git filter-repo` + force-push +
   alle aktiven Clones invalidieren
